# Experiments

Put short-lived proofs of concept here. Give each experiment its own directory and `package.json` so npm treats it as an independent workspace.

Delete experiments freely once they are no longer useful; move reusable code into `packages/`.
