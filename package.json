{
  "name": "sandbox-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "experiments/*"
  ],
  "scripts": {
    "start": "npm run start --workspace=@sandbox/hello",
    "test": "npm test --workspaces --if-present",
    "check": "npm run check --workspaces --if-present"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  }
}
